# Cloudflare IP Updater Configuration
# Copy this file to /etc/cloudflare-ip-updater/config and fill in your values
# Make sure the config file is readable only by root:
#   sudo cp /etc/cloudflare-ip-updater/config.default /etc/cloudflare-ip-updater/config
#   sudo chmod 600 /etc/cloudflare-ip-updater/config
#   sudo nano /etc/cloudflare-ip-updater/config

# Required: Your Cloudflare API token
# Get this from https://dash.cloudflare.com/profile/api-tokens
# Use "Edit zone DNS" template or create custom token with:
#   - Zone:Zone:Read
#   - Zone:DNS:Edit
CLOUDFLARE_API_TOKEN=your_api_token_here

# ---------------------------------------------------------------------------
# Optional settings
# ---------------------------------------------------------------------------
# CHECK_INTERVAL=180         # Check interval in minutes (default: 180 = 3 hours)
# RETRY_ATTEMPTS=3           # Retry attempts for API/IP checks (default: 3)
# VERIFY_DNS=0               # Verify DNS propagation after update (1/0, default: 0)
#
# After changing CHECK_INTERVAL, run: sudo cloudflare-ip-updater --setup-timer
# ---------------------------------------------------------------------------

# ---------------------------------------------------------------------------
# Multi-record mode: Add RECORD lines for each DNS record to update.
# Format: RECORD=domain:record_name:type
#   - domain: your domain (e.g., example.com)
#   - record_name: @ for root, or subdomain (e.g., home, vpn)
#   - type: A (IPv4) or AAAA (IPv6)
#
# Examples:
# RECORD=example.com:@:A
# RECORD=example.com:@:AAAA
# RECORD=example.com:home:A
# RECORD=example.com:home:AAAA
# RECORD=other.com:vpn:A
# ---------------------------------------------------------------------------
RECORD=example.com:@:A

# ---------------------------------------------------------------------------
# Legacy single-record mode (used only if no RECORD lines above):
# ---------------------------------------------------------------------------
# DOMAIN=example.com
# DNS_RECORD_NAME=@
# DNS_RECORD_TYPE=A
